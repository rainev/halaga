# Redis configuration for the valuation app.
# Backs the revocable refresh-token sessions (see backend session service).

# Cap memory, then evict least-recently-used keys.
maxmemory 256mb
maxmemory-policy allkeys-lru

# Lightweight persistence: snapshot to disk if data changed in the last 60s.
save 60 1
